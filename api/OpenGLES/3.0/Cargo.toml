[package]
name = "opengles_3_0"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[package.metadata.clock]
display_name = "OpenGL ES"
api_names = ["OpenGL ES", "OpenGLES", "GLES"]

[dependencies]
Clock = { path = "../../../core" }
